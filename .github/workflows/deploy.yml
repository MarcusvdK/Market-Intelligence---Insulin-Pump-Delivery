name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - '*.html'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Deploy to GitHub Pages
        run: |
          echo "Deploying HTML files to GitHub Pages..."
          # GitHub Pages automatically serves from root when configured
